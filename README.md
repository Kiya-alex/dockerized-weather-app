# Dockerized Weather App

A simple Python app that displays weather info for multiple cities,
fully containerized with Docker.

## Run with Docker

docker build -t weather-app .
docker run weather-app

## Run with Python

python app.py

## Technologies
- Python 3.11
- Docker
